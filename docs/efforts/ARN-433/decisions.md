## Decisions & Tradeoffs
No decisions - the file follows the established repo-contract shape; the
genesis-specific pass content derives from the repo's own contracts
(byte-exact smart-HTTP, GitHub REST subset, registry state machines).
