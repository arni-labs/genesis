# Spec
One file, the repo-contract shape shared with temperpaw/temper/dsf/katagami:
four passes, the meaning of Important, a nit cap, do-not-report rules -
each pass genesis-specific (the wire serves BYTES a real git must accept;
the registry must never diverge from git reality).
